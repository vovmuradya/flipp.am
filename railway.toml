# Specify the runtime
node.version = "22"
php.version = "8.4"

# Build command
[build]
  command = "composer install && php artisan migrate --force"

# Start command
[run]
  command = "sh start_railway.sh && php artisan serve --host=0.0.0.0 --port=$PORT"
gcloud compute ssh instance-20260115-091853 --zone us-central1-f
cd ~/flipp.am

 1 # 1. Получить последние изменения из репозитория
     git pull origin main

     # 2. Очистить кэшированные конфигурации
     php artisan optimize:clear

     # 3. Выполнить миграции базы данных (если есть новые)
     php artisan migrate

  1 # Получить хеш последнего коммита
    git rev-parse HEAD

    # Получить краткий хеш последнего коммита
    git rev-parse --short HEAD

   # Получить информацию о последнем коммите в одной строке
   git show --oneline -s

    # 4. Обновить зависимости Composer (если были изменения в composer.json)
    composer install --no-dev

    # 5. Очистить и скомпилировать ассеты (если были изменения в JS/CSS)
    npm install && npm run build

    # 6. Перезапустить службу очередей (если используется)
    php artisan queue:restart

    # 7. Перезапустить службу кэширования (если используется)
    php artisan cache:clear

    # 8. Перезапустить веб-сервер (если используется artisan serve)
    # или перезапустить ваш веб-сервер (nginx/apache)
    sudo systemctl restart nginx  # или ваш способ перезапуска веб-сервера

    # 9. Проверить статус всех служб
    php artisan config:cache
    php artisan route:cache
    php artisan view:cache

  Также рекомендую проверить работоспособность приложения после обновления:

   1 # Проверить статус приложения
    php artisan down  # временно отключить приложение для обслуживания
   3 # выполнить обновления
    php artisan up    # включить приложение обратно

  Для проверки логов в случае проблем:

   1 # Посмотреть последние логи
    tail -f storage/logs/laravel.log
   3
   4 # Проверить статус очередей
    php artisan queue:work --daemon










1. Сохранить код	На ПК	git add . и git commit -m "что сделал"
2. Отправить в облако	На ПК	git push origin main
3. Обновить сайт	На сервере	git pull origin main
4. Применить изменения	На сервере	php artisan optimize:clear
/////
 php artisan serve --host=0.0.0.0 --port=8002
 /////// cragiry bacelu hamar stex senc serve bacel

ssh root@idrom.am
ssh root@113.30.188.98

server password tatevS1000@1000


 CREATE USER IF NOT EXISTS 'flipp_user'@'127.0.0.1' IDENTIFIED BY 'vovaS1000@';
GRANT ALL PRIVILEGES ON flipp_am.* TO 'flipp_user'@'127.0.0.1';
FLUSH PRIVILEGES;
CREATE USER IF NOT EXISTS 'flipp_user'@'localhost' IDENTIFIED BY 'vovaS1000@';
GRANT ALL PRIVILEGES ON flipp_am.* TO 'flipp_user'@'localhost';
FLUSH PRIVILEGES;
EXIT;





craper/fetch-copart-cookies.cjs

git pull
git rev-parse HEAD

localum
./vendor/bin/sail up -d
git add .
git commit -m "Update"
git push

serverum
 cd /var/www/idrom
git fetch
git pull

git pull --ff-only
php artisan view:clear
npm ci && npm run build

git pull origin main
php artisan config:clear
php artisan cache:clear

tail -n 100 storage/logs/laravel.log

php artisan copart:update-cookies
cooki update

php artisan tinker


php artisan cache:clear
  php artisan config:clear
  php artisan view:clear

erb 500 oshibkaya cuyc tali pul ic heto

chown -R admin:admin storage
chmod -R 775 storage
chmod -R 775 bootstrap/cache



ЧТО ДЕЛАТЬ КОГДА ХОЧЕШЬ ИЗМЕНИТЬ КОД?
🧿 Шаг 1 — работаешь ТОЛЬКО локально

Открываешь проект в PhpStorm
(тот, который в WSL → \\wsl$ → Ubuntu → home → vov → flipp-am)

Делаешь любые изменения:
— правишь контроллер
— добавляешь новый Blade
— исправляешь баги
— что угодно

Это ЛОКАЛЬНАЯ разработка.

🧿 Шаг 2 — сохраняешь изменения в Git

После того как ты сделал изменения:

Выполняешь в WSL внутри проекта:
git add .
git commit -m "описание правок"
git push


Теперь эти изменения улетели на GitHub.

🧿 Шаг 3 — обновляешь ПРОДАКШЕН

Теперь идёшь на сервер:

ssh root@idrom.am


и выполняешь:

cd /home/admin/web/idrom.am/public_html
git pull
php artisan optimize:clear





Скрывать
Всегда храните свой токен в безопасности, чтобы защитить свой аккаунт. Подробнее

Вы используете пробную учётную запись и можете отправлять сообщения и звонить только на подтверждённые номера телефонов . Узнайте больше о пробной учётной записи.


 Вставляете строку cookies (например, из заголовка Cookie в DevTools) прямо в команду:

    php artisan copart:update-cookies "ocs=...; _abck=...; ak_bmsc=..."
  - Если аргумент не указать, команда запросит его интерактивно во время выполнения.
  - Значение в .env перезаписывается, а текущая конфигурация обновляется на лету, так что парсер сразу начнёт использовать свежие куки.



 Исправил x-data в resources/views/listings/_partials/filters.blade.php:40-69, теперь Alpine получает значения через @js(...) и выражение больше не рвётся; три кнопки «В Армении / За рубежом / В пути» прошиты напрямую,
    поэтому originOptions/searchTerm undefined больше не всплывают.
  - Добавлен контроллер app/Http/Controllers/MediaController.php и публичный маршрут media.show (routes/web.php:16-34), который читает файлы из storage/app/public и отдаёт их через Laravel. Карточка объявлений теперь строит
    ссылки через этот маршрут вместо прямого /storage/... (resources/views/components/listing/card.blade.php:22-90), так что изображения подгружаются даже когда веб‑сервер блокирует симлинк.
  - Очистил Blade-кэш после изменений (php artisan view:clear) чтобы шаблоны обновились.

  Перезагрузи http://localhost/?only_regular=1 — консоль Alpine должна быть чистой, а фото из конверсий грузятся без 403.   

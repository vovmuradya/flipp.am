 Вставляете строку cookies (например, из заголовка Cookie в DevTools) прямо в команду:

    php artisan copart:update-cookies "ocs=...; _abck=...; ak_bmsc=..."
  - Если аргумент не указать, команда запросит его интерактивно во время выполнения.
  - Значение в .env перезаписывается, а текущая конфигурация обновляется на лету, так что парсер сразу начнёт использовать свежие куки.
